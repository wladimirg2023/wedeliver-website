"use client";import{FormEvent,useState}from"react";const types=["Corporate Order","Catering","Condominium Partnership","Hotel Partnership","Bar / Venue Partnership","Supplier","Careers","General Inquiry"];export function ContactForm({defaultType="General Inquiry"}:{defaultType?:string}){const[status,setStatus]=useState<"idle"|"sending"|"sent"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus("sending");const f=e.currentTarget;try{const r=await fetch("/api/contact",{method:"POST",body:new FormData(f)});if(r.ok){f.reset();setStatus("sent")}else setStatus("error")}catch{setStatus("error")}}return <form onSubmit={submit} className="contact-form"><div className="field-grid"><label>Name<input name="name" required/></label><label>Company<input name="company"/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone" type="tel"/></label></div><label>Inquiry type<select name="type" defaultValue={defaultType}>{types.map(x=><option key={x}>{x}</option>)}</select></label><label>Message<textarea name="message" rows={6} maxLength={5000} required/></label><button className="button" disabled={status==="sending"}>{status==="sending"?"Sending…":"Send inquiry"}</button>{status==="sent"&&<p role="status">Thank you. Your inquiry has been sent.</p>}{status==="error"&&<p role="alert">We could not send your inquiry. Please try again.</p>}</form>}
